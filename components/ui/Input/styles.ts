import { StyleSheet } from "react-native";

export default StyleSheet.create({
    view:{
        alignItems: "flex-end", 
        marginBottom: 20
    },
    text:{
        fontSize: 48, 
        color: "#fff"
    }
})